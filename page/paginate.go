// A set of Elements are paginated into a set of optimal Pages
package page

import (
	"github.com/go-fountain/fountain/element"
)

func Paginate(elements element.Elements) (pages Pages) {
	// TODO(ckaye) paginate a set of elements into a set of optimal pages
	return
}
