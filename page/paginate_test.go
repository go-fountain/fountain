// A set of Elements are paginated into a set of optimal Pages
package page

import "testing"

func TestPaginate(t *testing.T) {
	// TODO(ckaye) test pagination of a set of elements into a set of pages
}
